/**
 * lito/HeroSection — re-exports the shared dark hero for lito registry use.
 * The shared HeroSection is already styled for the lito design system.
 */
export { HeroSection } from '../HeroSection'
